## Чат-бот в ТГ и автоматическое отслеживание цены на ноутбук из интернет-магазина


### Общий план Workflow:

1.  **Триггер "Расписание"**: Запускает весь процесс каждый день в нужное время.
2.  **Действие "HTTP Request"**: Загружает HTML-код страницы с ноутбуком.
3.  **Действие "HTML Content"** : "Вытаскивает" цену из HTML.
4.  **Действие "Telegram"**: Отправляет сообщение с ценой.

---

#### Шаг 0: Подготовка (сделать один раз)

1.  **Создать Telegram-бота**:
    *   Найти в Telegram `@BotFather`.
    *   Отправить ему команду `/newbot`.
    *   Дать боту имя (например, `PriceTrackerBot`).
    *   Записать **токен** доступа, который выдаст BotFather. Он выглядит примерно так: `1234567890:ABCDEFGhijKlMnOpQRsTuvWxyz...`
2.  **Узнать свой Chat ID**:
    *   Найти в Telegram созданного бота и написать ему любое сообщение (например, `/start`).
    *   Перейти по этой ссылке в браузере, подставив свой токен:  
        `(https://api.telegram.org/botТОКЕН/getMe)`ttps://api.telegram.org/botТОКЕН/getUpdates
        *   Должен открыться JSON-ответ. Найти там `"chat":{"id": ...}`.
        *   **Это число — ваш Chat ID.** Запишите его.

#### Шаг 1: Настройка Триггера "Расписание" (Schedule Trigger)

1.  В n8n создать новый workflow.
2.  Добавить ноду **"Schedule"**.
3.  Настройте её:
    *   **Trigger Interval**: `Every Day` (Каждый день)
    *   **Hour**: `10` (чтобы присылало, например, в 10 утра)
    *   **Minute**: `0`
    *   Остальное можно оставить по умолчанию.

#### Шаг 2: Получение страницы сайта (HTTP Request)

1.  Добавить ноду **"HTTP Request"** и соединить её с "Schedule".
2.  Настроить её:
    *   **Method**: `GET`
    *   **URL**: Вставить прямую ссылку на страницу ноутбука на выбранном сайте.
    *   **Send Headers?**: Включить.
    *   В **Headers** добавить строку:
        *   **Name**: `User-Agent`
        *   **Value**: `Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/91.0.4472.124 Safari/537.36`
        *   *Это нужно, чтобы сайт не подумал, что это бот, и не заблокировал запрос.*

#### Шаг 3: Извлечение цены из HTML

Цену нужно "вытащить" из HTML-кода. Для этого используется **CSS-селектор**.

**найти селектор для цены:**

1.  Открыть в браузере страницу с ноутбуком.
2.  Нажать **F12** (Инструменты разработчика).
3.  Нажать на значок "Выбрать элемент" (стрелка в квадрате).
4.  Кликнуть на цену на странице.
5.  В коде подсветится нужный элемент. Посмотреть на его атрибуты.
    *   *Пример 1:*
        ```html
        <div class="product-buy__price">99 999 ₽</div>
        ```
        Селектор будет: `.product-buy__price`
    *   *Пример 2:*
        ```html
        <span class="c303-a1">89 990 ₽</span>
        ```
        Селектор будет: `span.c303-a1`

**Настройка ноды HTML:**
1.  Выбрать "Extract HTML Content"! ✅.
* Настройка ноды "Extract HTML Content":
   * Data Property Name: data (то, что пришло из HTTP Request).
   * Source Data: JSON.
   * Extraction Values:
   * Key: price.
   * SS Selector: [ваш селектор для цены].
   * Return Value: Text.


#### Шаг 4: Отправка уведомления в Telegram

1.  Добавьте ноду **"Telegram"**.
2.  Настройте её:
    *   **Authentication**: `Выбрать созданного бота из списка`
    *   **Resource**: `Message`
    *   **Operation**: `Send Message`
    *   **Chat ID**: Тот самый **Chat ID**, который узнали на шаге 0.
    *   **Text**: Написать текст сообщения и вставить полученную цену.
        *   Например: `💰 Цена на ноутбук сегодня: {{ $json.price }}`
        *   `{{ $json.price }}` — это переменная, куда подставится значение из предыдущей ноды.
     
#### Настройка авторизации Telegram.

**Сredentials:**
1.  Нажать на ноду "Send a text message".
2.  Найти поле "Telegram Account" или "Credentials".
3.  Нажать "Add Credential" или "Create new credential".
4.  Выбрать "Telegram API".

**Настройка Telegram credentials:**
1.  Name: Дать любое имя (например, "My Price Bot").
2.  Access Token: Вставьте токен вашего бота (который вы получили от @BotFather).
3.  Нажать "Create" или "Save".

**После настройки credentials:**
1.  Выбрать созданные credentials в ноде "Send a text message".
2.  Заполнить остальные поля:
    Chat ID: Ваш числовой Chat ID
    Text: Цена: {{ $json.price }}



#### Финальный Workflow будет выглядеть так:

`[ Schedule ] -> [ HTTP Request ] -> [ HTML Extract ] -> [ Telegram ]`

---
<img width="1008" height="383" alt="Снимок экрана 2025-11-29 в 19 02 04" src="https://github.com/user-attachments/assets/ba350063-2439-435c-8eef-b0c03e5efd9d" />

