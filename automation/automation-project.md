Чат-бот в ТГ и автоматическое отслеживание цены на ноутбук из интернет-магазина


1.  Создание Telegram-бота:
    Найти в Telegram `@BotFather`.
    Отправить ему команду `/newbot`.
    Дать боту имя (например, `PriceTrackerBot`).
    Записать **токен** доступа, который выдаст BotFather. Он выглядит примерно так: `1234567890:ABCDEFGhijKlMnOpQRsTuvWxyz...`
2.  Узнать свой Chat ID:
    Найти в Telegram созданного бота и написать ему любое сообщение (например, `/start`).
    Перейти по этой ссылке в браузере, подставив свой токен:  
    `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
    Должен открыться JSON-ответ. Найти там `"chat":{"id": ...}`.
    **Это число — Chat ID.** Сохранить его.

#### Шаг 1: Настройка Триггера "Расписание" (Schedule Trigger)

1.  В n8n создайте новый workflow.
2.  Добавьте ноду **"Schedule"**.
3.  Настройте её:
    *   **Trigger Interval**: `Every Day` (Каждый день)
    *   **Hour**: `10` (чтобы присылало, например, в 10 утра)
    *   **Minute**: `0`
    *   Остальное можно оставить по умолчанию.

#### Шаг 2: Получение страницы сайта (HTTP Request)

1.  Добавьте ноду **"HTTP Request"** и соедините её с "Schedule".
2.  Настройте её:
    *   **Method**: `GET`
    *   **URL**: Вставьте прямую ссылку на страницу ноутбука на выбранном сайте (например, из DNS, Ситилинк, Ozon и т.д.).
    *   **Send Headers?**: Включите.
    *   В **Headers** добавьте строку:
        *   **Name**: `User-Agent`
        *   **Value**: `Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/91.0.4472.124 Safari/537.36`
        *   *Это нужно, чтобы сайт не подумал, что это бот, и не заблокировал запрос.*

> **⚠️ Важное предупреждение:** Парсинг сайтов может нарушать их правила. Используйте это для личных, некоммерческих целей и не делайте слишком частые запросы, чтобы не нагружать серверы.

#### Шаг 3: Извлечение цены из HTML (Самое сложное)

Цену нужно "вытащить" из HTML-кода. Для этого используется **CSS-селектор**.

**Как найти селектор для цены:**

1.  Откройте в браузере страницу с ноутбуком.
2.  Нажмите **F12** (Инструменты разработчика).
3.  Нажмите на значок "Выбрать элемент" (стрелка в квадрате).
4.  Кликните на цену на странице.
5.  В коде подсветится нужный элемент. Посмотрите на его атрибуты.
    *   *Пример из DNS:*
        ```html
        <div class="product-buy__price">99 999 ₽</div>
        ```
        Селектор будет: `.product-buy__price`
    *   *Пример из Ozon:*
        ```html
        <span class="c303-a1">89 990 ₽</span>
        ```
        Селектор будет: `span.c303-a1`

**В n8n есть два основных способа это сделать:**

**Способ A (Проще, через "HTML Extract"):**

1.  Добавьте ноду **"HTML Extract"**.
2.  Настройте:
    *   **Data Property Name**: `data` (значение по умолчанию, там лежит HTML от предыдущей ноды).
    *   **Source Data**: `JSON`
    *   **Extraction Values**:
        *   **Key**: `price` (название для найденного значения).
        *   **CSS Selector**: Тот самый селектор, который вы нашли (например, `.product-buy__price`).
        *   **Return Value**: `Text`

**Способ B (Гибче, через "Code"):**

Если "HTML Extract" не сработал, можно использовать код.

1.  Добавьте ноду **"Code"** (раздел "Core Nodes").
2.  Выберите **"JavaScript"**.
3.  Вставьте такой код, подставив свой селектор:

```javascript
// Загружаем библиотеку для парсинга HTML (в n8n она уже есть)
const cheerio = require('cheerio');

// Загружаем HTML, который пришёл из предыдущей ноды "HTTP Request"
const html = $('data').body;

// Инициализируем парсер
const $el = cheerio.load(html);

// Используем ваш CSS-селектор, чтобы найти цену
const price = $el('ВАШ_SELECTOR_ЗДЕСЬ').text().trim();

// Возвращаем результат в следующую ноду
return [{ json: { price: price } }];
```

#### Шаг 4: Отправка уведомления в Telegram

1.  Добавьте ноду **"Telegram"**.
2.  Настройте её:
    *   **Resource**: `Message`
    *   **Operation**: `Send Message`
    *   **Chat ID**: Тот самый **Chat ID**, который вы узнали на шаге 0.
    *   **Text**: Напишите текст сообщения и вставьте полученную цену.
        *   Например: `💰 Цена на ноутбук сегодня: {{ $json.price }}`
        *   `{{ $json.price }}` — это переменная, куда подставится значение из предыдущей ноды.

#### Финальный Workflow будет выглядеть так:

`[ Schedule ] -> [ HTTP Request ] -> [ HTML Extract ] -> [ Telegram ]`

---

### Пример готовой настройки для сайта DNS (условно)

1.  **Schedule**: Каждый день в 10:00.
2.  **HTTP Request**:
    *   URL: `https://www.dns-shop.ru/product/.../noutbuk-.../`
    *   Headers: `User-Agent: Mozilla/5.0...`
3.  **HTML Extract**:
    *   Key: `price`
    *   CSS Selector: `.product-buy__price`
4.  **Telegram**:
    *   Text: `🖥️ Цена на ноутбук ASUS X515 сегодня: {{ $json.price }}. Ссылка: https://...`

### Советы и возможные проблемы

*   **Тестируйте!** Запускайте workflow вручную (кнопка "Execute Workflow"), чтобы убедиться, что на каждом шаге есть данные.
*   **Селекторы ломаются.** Если дизайн сайта обновился, селектор перестанет работать, и вы будете получать пустые сообщения. Придётся найти его заново.
*   **Для динамических сайтов** (где контент подгружается через JavaScript) простой "HTTP Request" может не сработать. В этом случае придется использовать более сложные ноды, например, "Puppeteer", которая управляет реальным браузером.

Это базовая, но мощная автоматизация. Как только она заработает, вы сможете легко добавить отслеживание цен для других товаров!
